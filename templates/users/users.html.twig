<!DOCTYPE html>
<html>

<head>
    <title>User-Profile</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/axentix@2.4.0/dist/axentix.min.css">
</head>

<body class="layout-sidenav">
    <header>
      
        <nav class="navbar shadow-1 success">
           
        <a class="navbar-brand text-brand" href="{{path('app_home')}}">FreeImmo</a>
            <div class="navbar-menu ml-auto">
            <a class="nav-link text-decoration-none" href="{{path('app_home')}}">Accueil</a>
                <a class="navbar-link" href="{{ path('app_logout') }}">Déconnexion</a>
            </div>
            
        </nav>
       
    </header>

    <div id="example-sidenav" data-ax="sidenav" class="sidenav shadow-1 sidenav-fixed white">
        
        <a href="#" class="sidenav-link active">Mon Profile</a>
        <a href="#" class="sidenav-link"></a>
        <a href="#" class="sidenav-link"></a>
        <a href="#" class="sidenav-link"></a>
    </div>

    <main>
        <!-- Code here the main content -->

        <div class="grix xs4 container m-4">
            <div class="col-xs4 m-4"><h1> Profile de : {{app.user.prenom}} {{app.user.nom}}</h1></div>
            <div>Nom : </div>
            <div>{{app.user.nom}}</div>
            <div>| Prenom :</div>
            <div>{{app.user.prenom}}</div>
            <div>E-mail :</div>
            <div>{{app.user.email}}</div>
            <div>| Adresse :</div>
            <div>{{app.user.Adresse}}</div>
            <div>Mobile :</div>
            <div>{{app.user.telephoneportable}}</div>
            <div>| Ville :</div>
            <div>{{app.user.ville}}</div>
            <div>Entreprise :</div>
            <div>{{app.user.entreprise}}</div>
            <div>| Code-Postale:</div>
            <div>{{app.user.codepostale}}</div>
            <div>Telephone-Fixe :</div>
            <div>{{app.user.telephonefixe}}</div>
            <div> | Site-Web :</div>
            <div>{{app.user.siteweb}}</div>
           
<div class="col-xs4 m-4">
    <a href="#" class="btn shadow-1 rounded-1 blue">Modifier le Profile</a>
    <a href="#" class="btn shadow-1 rounded-1 blue">Modifier le mot de passe</a>
</div>

<div class="col-xs4 m-4">
    <h4> Annonces de : {{app.user.prenom}} {{app.user.nom}} ({{ app.user.propertys|length }})</h4>
</div>

{% for property in app.user.propertys %}
    <div> | Reference :</div>
    <div>{{property.reference}}</div>
{% endfor %}



        </div>
        <button data-target="example-sidenav" class="btn rounded-1 press amaranth dark-1 hide-md-up">
            Open sidenav
        </button>
    </main>

    <footer class="footer success">
        Copyright © 2024 - FreeImmo
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/axentix@2.4.0/dist/axentix.min.js"></script>
</body>

</html>